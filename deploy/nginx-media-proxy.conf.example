# Example nginx location block for the React app (mycafe.sewabyapar.com).
# Add this so /media-proxy/ is proxied to the backend /media/ path, avoiding CORS
# when the frontend fetches logos (e.g. on the QR page).
#
# Add this location inside your server { } block for mycafe.sewabyapar.com,
# e.g. right after the location /api/ block.
#
# Replace 9093 with the port where your backend (Django) listens if different.
# After editing, reload nginx: sudo nginx -t && sudo nginx -s reload

# Media proxy: same-origin so no CORS (browser -> mycafe -> backend /media/)
location /media-proxy/ {
    proxy_pass http://127.0.0.1:9093/media/;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}
